# AUDIT-2026-08-29｜dsh-come 架构与实现审计

> 范围：src/*.rs（14 模块）、dist/admin.html（管理页）、跨平台支持度、方向 v4 的边界执行度。
> 前提：用户已拍板 ① 支持跨平台；② 「壳零 UI」可突破但要注意边界。
> 方法：源码通读 + 平台分支统计 + 关键调用链闭合验证（spawn→进程组→杀树、HTTP 路由→副作用端点）。

## 摘要

| 等级 | 数量 | 主题 |
|---|---|---|
| 🔴 P0 | 3 | 本地 API 无来源校验（可被任意网页卸载 dsh 并删数据）／Unix 杀树语义错配（会杀掉用户终端会话）／自更新换装脚本 Unix 下必然失败且误报成功 |
| 🟡 P1 | 5 | patch YAML 双解析器／壳越界读写 dsh 内部文件／管理页 HTML 热加载后门／HTTP 层健壮性／跨平台路径与目录名 |
| 🔵 P2 | 5 | stop 延迟／doctor 依赖外部命令／主题探测／单实例锁语义耦合／托盘平台降级 |

---

## 🔴 P0-1：管理页 API 无任何来源校验，任意网页可卸载 dsh 并删除数据

- **实现方式**：`status.rs` 用裸 `TcpListener` 手写 HTTP，只解析请求行的 method + path，**完全不读请求头**。
- **位置**：`src/status.rs:63-88`（handle，只 `lines.next()` 取请求行）；全部写端点在 `:154-208`（插件装卸、dsh 卸载、启停）。
- **原因**：设计假设「只监听 127.0.0.1 所以安全」。但 localhost **不是安全边界**——浏览器对 `http://127.0.0.1:3081` 的简单请求（POST 且无自定义头）不触发 preflight，会直接发出。响应读不到无所谓，**副作用已经发生**。
- **可达后果**：任意恶意/被投毒页面执行
  `fetch('http://127.0.0.1:3081/api/dsh/uninstall?keepData=0&cleanShim=1',{method:'POST'})`
  → `uninstall.rs` 卸载系统 dsh 并删除 `%USERPROFILE%\.dsh`（凭据/配置/工作台数据）。同理可 `/api/plugin/uninstall/*` 破坏配置、`/api/stop` 停引擎。叠加 P1-3 的 HTML 热加载后门，构成完整本地提权链。
- **是否违反原则**：是。突破「零 UI」的同时没有建立对应的安全边界——**能力扩张了，护栏没跟上**。
- **修复**：
  1. 所有非 GET 端点校验 `Origin`/`Host` 必须是 `127.0.0.1:<port>` 或 `localhost:<port>`；两者都缺失时拒绝（现代浏览器同源 fetch 必带 Origin）。
  2. 危险操作（卸载 dsh、删数据、插件装卸）要求一次性 CSRF token：启动时生成随机数写入运行时状态，页面从 `/api/status` 读取并在请求头回传。
  3. 响应补 `Vary: Origin`、`X-Content-Type-Options: nosniff`。

## 🔴 P0-2：Unix 下 kill_tree 对「外部认领进程」会杀掉整个终端进程组

- **实现方式**：Windows 分支 `taskkill /T /F /PID`（按进程**树**杀）；Unix 分支 `kill(-(pid as i32), SIGTERM)`（按进程**组**杀）。
- **位置**：`src/supervisor.rs:843-869`（kill_tree）；调用点 `:489`（kill_child）、`:706`（认领探活判死后接管）、`:751`（页面探活判死）。
- **原因**：两个平台语义不等价。Unix 分支隐含假设 `pgid == pid`，该假设**只在引擎由壳 spawn 时成立**——`build_command:334` 调 `start_process_group`（`:221-224`，`process_group(0)`）使子进程自成一组的组长，此时 pid == pgid ✓。但外部认领的 dsh 是用户在终端里 `dsh web` 启的，它的 pgid 是**那个终端会话的进程组**，pid != pgid。
- **后果**：macOS/Linux 上，用户从终端启动 dsh → 壳认领（owned=false）→ 点托盘「关闭引擎」→ `kill_child:489` → `kill(-pgid)` → **杀掉该终端会话里的所有进程**（bash、正在跑的其他命令）。README 明确写「关闭引擎不区分是否本壳启动」，所以这条路径必然被触发。
- **是否违反原则**：是，属跨平台适配的语义错配，非设计意图。
- **修复**：Unix 下先 `libc::getpgid(pid)`，**仅当 `getpgid(pid) == pid` 才组杀**；否则退回单进程 `kill(pid, SIGTERM)`，并递归枚举子进程（Linux 读 `/proc/*/stat` 的 ppid，macOS 用 `sysctl KERN_PROC_ALL`）。这个判定应放进 `kill_tree` 内部，让三个调用点都自动正确。

## 🔴 P0-3：自更新换装脚本是 PowerShell，Unix 分支用 `sh` 执行它

- **实现方式**：`install()` 生成 `swap-update.ps1`，内容为硬编码 PowerShell（`schtasks /Change /TN 'DSH伴侣守护'`、`Copy-Item`、`Move-Item`、`Start-Process`）；Windows 用 `powershell -File` 执行，Unix 用 `sh <script>` 执行同一个 .ps1。
- **位置**：`src/updater.rs:176-222`（脚本生成）、`:213-221`（平台分支）。
- **原因**：Unix 分支只是套了个壳占位，脚本内容没有跟着平台换。
- **后果**：macOS/Linux 点「更新到 vX」→ sh 执行 PowerShell 语法必然失败 → **新 exe 已下载校验但未替换**，而 `install()` 仍返回 `Ok(())` → UI 显示更新成功，用户以为已更新。看门狗禁用/恢复也依赖 schtasks，Unix 无对应实现。
- **是否违反原则**：是。自更新是 P0 已交付功能，跨平台下是坏的（且是**静默失败**）。
- **修复**：拆成两份脚本模板（`.ps1` / `.sh`），看门狗控制一并平台化（Windows schtasks／macOS launchd／Linux systemd user unit）。替换失败必须回传错误码，不要吞掉子进程结果。

---

## 🟡 P1-1：cordis.patch.yml 有两套独立的手写 YAML 解析器

- **位置**：`src/doctor.rs:866-965`（`top_level_entry_ranges`/`entry_id_of`/`orphan_file_entries`/`remove_entry`/`looks_like_patch`）vs `src/status.rs:337-375`（`push_patch_item`/`parse_patches`）。
- **实现方式**：都靠缩进 + 字符串匹配做 YAML「手术」，无 yaml 库（Cargo.toml 确实无 yaml 依赖）。
- **原因**：doctor 先写，status 后加，各写一套。
- **后果**：对同一文件可能给出不同答案（嵌套条目、注释、`- id:` 前导空格差异、CRLF）。改一处不改另一处 → **自愈与管理页对同一配置的判断互相矛盾**，而两者都会动这个文件。
- **是否违反原则**：不违反边界，是明显的重复实现。
- **修复**：抽 `src/patchyml.rs` 统一解析与保序编辑，两边共用；或引入 serde_yaml 做真解析 + 保序编辑（推荐后者，手写 YAML 迟早出事）。

## 🟡 P1-2：壳直接读写 dsh 内部文件，越过「只碰门把手」边界

- **位置**：`src/status.rs:316-320`（`profile_dir()` = `$DSH_HOME/profiles/web`）、`:323-336`（`parse_bundles` 读 `package.json` 的 `dsh.profile.bundles`——**解析 dsh 内部数据结构**）、`:408-462`（`uninstall_patch` 直接编辑 `cordis.patch.yml`）、`:464-488`（`uninstall_bundle` 走 CLI）。
- **原因**：管理页做插件管理时走了文件层捷径；注意 `uninstall_bundle` 已经正确走了 CLI，说明同功能两种实现并存。
- **后果**：dsh 改 `package.json` schema 或 patch 布局 → 壳静默失效或写坏用户配置。
- **是否违反原则**：**违反**。`docs/cli-contract.md` 写明「不解析 CLI 输出、不读内部文件」，且契约里 C5「插件管理」仍是**预留**状态——壳已先行实现，却没走契约。
- **修复（边界建议）**：装卸一律走 CLI 契约（`dsh plugin --profile web remove/add`，照 `uninstall_bundle` 的样子），并把 C5 正式写进 cli-contract；读列表如需保留读文件，集中在 patchyml.rs 并加 schema 版本校验，**不匹配时禁用该功能而非猜测**。

## 🟡 P1-3：管理页 HTML 的「exe 同级热加载」是生产面后门

- **位置**：`src/status.rs:513-525`——`admin_html()` 先找 `current_exe()` 同级的 `admin.html`，存在就读它，否则用 `include_str!`。
- **原因**：开发期免重编译（注释已写明）。
- **后果**：生产环境下任何人往 exe 目录放一个 `admin.html` 即可注入任意 JS 到管理页，而管理页能调卸载/启停 API → 与 P0-1 叠加成完整提权链；且行为随环境残留文件而不可预测。
- **修复**：热加载分支用 `#[cfg(debug_assertions)]` 包住，release 构建只走 `include_str!`。

## 🟡 P1-4：HTTP 层健壮性（4096 缓冲、只读一次、无限线程）

- **位置**：`src/status.rs:64-83`。
- **实现方式**：`buf = [0u8; 4096]`、只读一次、每连接 `thread::spawn`。
- **后果**：① 长 URL（`/api/plugin/install?src=<长路径>`）可能被截断成半个路径；② 请求头跨 TCP 段时解析不全；③ 无并发上限，本地洪水可耗尽线程。
- **修复**：循环读到 `\r\n\r\n`（上限 64KB，超限 431）；加全局并发信号量。

## 🟡 P1-5：跨平台路径比较与目录名的正确性

- **位置**：`src/uninstall.rs:222-233`（`same_path` 用 `to_lowercase()` 比较）、`:206`（shim 候选名仅 Windows 扩展名）；`src/runtime.rs:28,34,37`（数据目录名仍是 `dsh-desktop`）。
- **后果**：Unix 路径大小写敏感，`to_lowercase()` 会把 `/usr/local/bin/Dsh` 与 `/usr/local/bin/dsh` 误判为同一路径 → 跳过本该清理的 shim。目录名 `dsh-desktop` 是两度更名前的遗留，与 `DSH_DESKTOP_HOME` 一起构成命名噪音。
- **修复**：`same_path` 按平台分支，Unix 用严格 `PathBuf` 比较；**跨平台正式发布前**一次性改目录名（带旧目录迁移逻辑），发布后就改不动了。

---

## 🔵 P2

| # | 位置 | 问题与建议 |
|---|---|---|
| 1 | `main.rs:195-212` → `supervisor.rs:569` | `dsh-come stop` 写 control.json，监测线程每 1s 才消费 → 最多 1s 延迟，脚本里紧跟 `status` 会读到旧值。建议 request_stop 后轮询 state.json（≤2s）再返回 |
| 2 | `doctor.rs:758-818`、`:651`/`:705` | 进程/端口枚举依赖 `ps`/`lsof` 外部命令，容器与精简系统常缺失，且 BSD/BusyBox 输出格式不同。建议 Linux 直接读 `/proc/net/tcp` + `/proc/*/fd`，命令缺失时把探针记为 **unknown** 而非静默返回空 |
| 3 | `tray.rs:703-706` | `is_light_theme()` 非 Windows 恒返回 `true`，浅色/深色不跟随。macOS 可读 `NSUserDefaults`，Linux 读 `gsettings`/`GTK_THEME`；读不到再回落 true |
| 4 | `main.rs:161-192` | 单实例锁被用于推断「守护是否在跑」（`status` 子命令靠抢锁失败判断）。锁创建失败时 Windows 分支放行返回 true（`:59-61`）→ `status` 误报「守护未运行」。建议 status 读 state.json 心跳时间戳判断存活 |
| 5 | `tray.rs:401-402`、`:84-86` | Unix 下幽灵图标自愈（Explorer PID 监听）为空实现。Linux 托盘本就碎片化（GNOME 已移除传统 tray、需 libayatana-appindicator），建议 Linux 把 `--no-tray` + systemd user service 作为一等形态，托盘只做 best-effort |

---

## 边界建议

### 一、「零 UI」可突破，但边界线要画在「引擎起不起得来」

判断标准一句话：**凡是 dsh 在跑的时候就能做的事，都不该在壳里做。**

- **正当留在壳内**（引擎起不来时必须有）：环境探测与安装引导（node/dsh）、引擎启停、日志与 doctor 诊断报告、自更新。
- **应当迁出**（dsh 活着就能做，且 dsh-market 插件已覆盖）：插件清单与装卸、dsh 版本管理、插件市场。
- **落地方式**：管理页检测到引擎就绪后，把这些操作**跳转到 dsh web 对应页面**，而不是自己做一遍。这样管理页自然收敛成「预启动/救急入口」，符合方向 v4 的鸡生蛋论证，也不会随功能增长膨胀。

按这条线，当前管理页的插件装卸与版本管理属于**越线**——它们既违反契约（P1-2），也是 UI 膨胀的起点。已拍板的「管理页保留在壳内」不必推翻，只需把壳内页面**收缩到救急面**。

### 二、跨平台推进的边界

- **已经做对的**（可直接沿用）：单实例（mutex/flock）、进程组（`process_group(0)` + `kill -pgid` 对自有引擎正确）、路径（XDG/HOME/LOCALAPPDATA）、打开浏览器与目录、通知、卸载主流程。这些分支是完整实现而非占位，说明跨平台基础是扎实的。
- **必须补齐才能宣布支持跨平台**：P0-2（杀树语义）、P0-3（自更新与看门狗的 launchd/systemd 对应物）、安装分发形态（Windows 单 exe；Unix 需 brew tap / deb-rpm / 安装脚本三选一）。
- **建议降级为 best-effort**：托盘。它是平台差异最大的面（tray.rs 共 21 处平台分支），在 Linux 上投入产出比低。
- **不要在跨平台这一轮顺手做**：目录改名可以（发布前最后窗口），但插件管理重写、管理页框架化（引入 axum/tiny-http 替换手写路由）等重构应与跨平台解耦，分两个 PR 走，避免跨平台回归无法定位。

### 三、过度开发风险提示

P0-1 的修复不要扩展成「给管理页做一套鉴权体系」——加 Origin 校验 + 危险操作一次性 token 即可，这是本地单机工具。同理，P1-1 统一 YAML 解析是为了消除两套解析器的不一致，不是为了做通用 YAML 编辑器。

---

## 修复进度（2026-08-29 跨平台收尾轮后）

**结论：审计全部条目已闭环，测试 53/53 通过、零警告。** 跨平台三件套（P0-3 看门狗对应物 / P2-5 无头一等形态 / Unix 分发形态）本轮补齐：

| 条目 | 实现方式 | 位置 |
|------|----------|------|
| P0-3 剩余：Unix 换装补看门狗停/复 | 换装脚本步骤 0 停看门狗（`launchctl bootout` / `systemctl --user stop`）、步骤 4 恢复（bootstrap / start）；失败 `|| true` 不阻断换装 | `src/updater.rs:209-277`（`unix_swap_script` 新参数）、`:282-303`（`watchdog_control()` 三平台分支）、`:327-340`（`install()` 传参） |
| P0-3 看门狗注册脚本（launchd/systemd 对应物） | macOS LaunchAgent（KeepAlive + RunAtLoad）；Linux systemd user unit（Restart=always + 60s 退避，`ExecStart=… --no-tray`）；`install`/`remove` 双动作；非 systemd 环境显式降级提示 | `scripts/install-watchdog.sh`（新建，与 `install-watchdog.ps1` 同语义） |
| P2-5：Linux `--no-tray` 一等形态 | `--no-tray` 早已实现（`main.rs:327-338,417-437` `headless_loop`）；本轮 systemd user service 即以其为一等启动形态 | `scripts/install-watchdog.sh:86-135`（install_linux/remove_linux） |
| Unix 分发形态（三选一：安装脚本） | `scripts/install.sh`：平台判定 → 拉 update-{macos,linux}.json → 下载 + sha256 校验 → 装 `~/.local/bin` → 注册看门狗；支持 curl/wget 管道安装 | `scripts/install.sh`（新建） |
| 分发 CI 三平台矩阵 | release.yml 四构建（win / macos-x64 / macos-arm64 / linux）→ lipo universal → 按平台生成 update-{win,macos,linux}.json → 附带旧版兼容 `update.json` + 两个安装脚本 | `.github/workflows/release.yml`（重写为矩阵） |
| 更新清单按平台分发 | `updater.rs` `check()` 按 `platform_suffix()` 取 `update-{win\|macos\|linux}.json`；下载路径 Unix 用 `<exe>.new` | `src/updater.rs:70-78`（`platform_suffix`）、`:134-137`（check URL）、`:171-175`（.new 扩展名） |
| 文档 | 架构文档「稳定性」条目扩为三平台 + 新增「看门狗与分发」章节 | `docs/architecture.md:85-106` |

**遗留（有意不做，非缺陷）**：
- brew tap / deb-rpm 分发形态未做——已按审计边界建议选「安装脚本」路线（三选一取一），后续有需要再补。
- 换装脚本内的看门狗命令为 `|| true` 兜底：无看门狗的环境也允许更新，仅留日志——这是设计取舍，不是静默失败（脚本全程写 `swap-update.log`）。
