# docs/archive — 时点档案

只读档案：已完成闭环的时点报告与设计快照。正式的决策日志在根 `MEMORY.md` 与 `docs/memory.md`；
当前架构描述见 `docs/architecture.md`（自封唯一权威）。本目录内容不再随代码演进更新。

- `AUDIT-2026-08-29.md`：v1.4.0 发布前的架构与实现审计，全部条目已闭环（commit `abdd4fa`）。
